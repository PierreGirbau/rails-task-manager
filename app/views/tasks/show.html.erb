<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<% if @task.completed == true %>
  <h3><%= 'This task is completed' %></h3>
<% else %>
  <h3><%= 'This task is not completed yet' %></h3>
<% end %>
<%= link_to 'Back to list', tasks_path(@tasks) %></li>
